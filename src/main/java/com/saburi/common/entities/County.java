/*
 Sam Buriima
generated by Saburi Tools
 */
package com.saburi.common.entities;

import javax.persistence.Entity;
import javax.persistence.Id;
import javax.persistence.JoinColumn;
import javax.persistence.ForeignKey;
import javax.persistence.OneToOne;
import javax.persistence.Column;

@Entity
public class County extends DBEntity {

    @Column(name = "idHelper", updatable = false)
    private int idHelper;
    @OneToOne
    @JoinColumn(name = "districtID", foreignKey = @ForeignKey(name = "fkDistrictCounty"), nullable = false)
    private LookupData district;
    @Id
    @Column(name = "countyID", updatable = false, length = 200, nullable = false)
    private String countyID;
    @Column(name = "countyName", length = 100, nullable = false)
    private String countyName;

    public County() {
    }

    public County(int idHelper, LookupData district, String countyID, String countyName) {
        this.idHelper = idHelper;
        this.district = district;
        this.countyID = countyID;
        this.countyName = countyName;

    }

    public int getIdHelper() {
        return idHelper;
    }

    public void setIdHelper(int idHelper) {
        this.idHelper = idHelper;
    }

    public LookupData getDistrict() {
        return district;
    }

    public void setDistrict(LookupData district) {
        this.district = district;
    }

    public String getCountyID() {
        return countyID;
    }

    public void setCountyID(String countyID) {
        this.countyID = countyID;
    }

    public String getCountyName() {
        return countyName;
    }

    public void setCountyName(String countyName) {
        this.countyName = countyName;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (!(o instanceof County)) {
            return false;
        }

        County county = (County) o;

        return this.getId().equals(county.getId());
    }

    @Override
    public int hashCode() {
        return this.countyID.hashCode();

    }

    @Override
    public Object getId() {
        return this.countyID;
    }

    @Override
    public String getDisplayKey() {
        return this.countyName;
    }

}
